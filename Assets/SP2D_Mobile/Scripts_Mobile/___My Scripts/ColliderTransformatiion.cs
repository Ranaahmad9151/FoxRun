using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColliderTransformatiion : MonoBehaviour
{
/*    public  Collider2D ground;
    public  Collider2D wall;
    public static bool isUpperGround=false;
    // Start is called before the first frame update
    void Start()
    {
      //  ground = GetComponent<Collider2D>();
      //  wall = GetComponent<Collider2D>();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    private void OnTriggerEnter2D(Collider2D collision)
    {
        if(collision.gameObject.tag=="Player")
        {
            wall.GetComponent<Collider2D>().enabled = false;
            ground.GetComponent<Collider2D>().enabled = true;
        }
    }
    private void OnTriggerExit2D(Collider2D collision)
    {
        if(collision.gameObject.tag=="Player"&& isUpperGround==true)
        {
            wall.GetComponent<Collider2D>().enabled = true;
            ground.GetComponent<Collider2D>().enabled = false;
        }
    }*/
}
