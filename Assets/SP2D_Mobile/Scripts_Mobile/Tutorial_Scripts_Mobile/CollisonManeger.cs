using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Bitboys.SuperPlaftormer2D {
public class CollisonManeger : MonoBehaviour {


	// Use this for initialization
	void Start () {
		
	}
	
	void OnTriggerEnter2D(Collider2D trigger)
	{
		//Debug.Log ("Show Tutorial Collison");
		//FindObjectOfType<TutorialManeger>().StartTutorial(trigger.gameObject.name);
		
//		if ( trigger.name = ")
//			{
//			Debug.Log ("Show Tutorial");
//			GameObject.Find ("Tutorial").transform.GetChild (0).gameObject.SetActive (true);
//			//FindObjectOfType<TutorialManeger> ().transform.GetChild (0).gameObject.SetActive (true);
//			}
	}
}
				}
